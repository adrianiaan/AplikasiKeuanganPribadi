package model;

/**
 * Enum Kategori untuk mengelompokkan jenis transaksi.
 * Enum ini berisi dua kategori utama:
 * - Pemasukan: untuk transaksi yang menambah uang.
 * - Pengeluaran: untuk transaksi yang mengurangi uang.
 */
public enum Kategori {
    Pemasukan,   // Kategori untuk transaksi pemasukan
    Pengeluaran; // Kategori untuk transaksi pengeluaran
}
